name: building sturdynet packages

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: self-hosted
    container: sturdynetoci/docker-openwrt-sdk:22.03.1-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: mkdir "${{ github.workspace }}/bin" && cd "${{ github.workspace }}/bin" && pwd
#       - run: export MAKEFLAGS="${MAKEFLAGS} OUTPUT_DIR=${PWD}/bin -s"
#       - run: echo ${MAKEFLAGS}
#       - run: echo ${PWD}
#       - run: cd flow_broker && ls && cd ..
#       - run: /add-custom-local-feed.sh .
#       - run: /install-custom-feed-packages.sh
#       - run: /reconfigure.sh
#       - run: /make-custom-feed-packages.sh
#       - run: /make-index.sh "${{ github.workspace }}/key-build"
#       - run: mkdir -p "${{ github.workspace }}/public/${{ github.head_ref }}"
#       - run: /generate-repository.sh "${{ github.workspace }}/public/${{ github.head_ref }}" "${{ github.workspace }}/key-build.pub" "${{ github.workspace }}/bin" "${{ github.workspace }}/47bc41ad54a6601d"
#       - run: mv LICENSE "${{ github.workspace }}/public/${{ github.head_ref }}"
#       - run: ./generate-webpage.sh "${{ github.workspace }}/public/${{ github.head_ref }}" > "${{ github.workspace }}/public/${{ github.head_ref }}/index.html"
#       - name: Use the Upload Artifact GitHub Action
#         uses: actions/upload-artifact@v2
#         with: 
#           name: ${{ github.head_ref }}
#           path: public

#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2
#       - name: Build and push
#         uses: docker/build-push-action@v4
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/docker-openwrt-sdk:22.03.1-latest
